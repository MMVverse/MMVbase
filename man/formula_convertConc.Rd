% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translational.R
\name{formula_convertConc}
\alias{formula_convertConc}
\title{Formula converting concentrations between matrices and free and total}
\usage{
formula_convertConc(
  Cin,
  from = c("blood", "plasma", "unbound", "medium")[1],
  to = c("blood", "plasma", "unbound", "medium")[2],
  Rbp_from = 1,
  PPB_from = 0,
  Rbp_to = 1,
  PPB_to = 0
)
}
\arguments{
\item{Cin}{input concentration (numeric)}

\item{from}{source matrix (character), must be one of "blood", "plasma", "unbound", or "medium"}

\item{to}{target matrix (character), must be one of "blood", "plasma", "unbound", or "medium"}

\item{Rbp_from}{blood to plasma ratio for the source matrix (numeric), must be greater than 0}

\item{PPB_from}{plasma protein binding for the source matrix (numeric), must be a value between 0 and 1 (inclusive).}

\item{Rbp_to}{blood to plasma ratio for the target matrix (numeric), must be greater than 0}

\item{PPB_to}{plasma protein binding for the target matrix (numeric), must be a value between 0 and 1 (inclusive).}
}
\value{
numeric, converted concentration
}
\description{
Formula converting concentrations between matrices and free and total
}
\examples{
# Example 1: Convert concentration from blood to plasma
# formula_convertConc converts concentration from blood to unbound then to plasma 
Cin <- 10
Rbp_from <- 2
PPB_from <- 0.1
Rbp_to <- 0.8
PPB_to <- 0.2

expected_unbound <- Cin * (1 - PPB_from) / Rbp_from
result_unbound <- formula_convertConc(Cin, from = "blood", to = "unbound", 
                                      Rbp_from, PPB_from, Rbp_to, PPB_to)

print(c(result_unbound, expected_unbound))

expected_plasma <- result_unbound / (1 - PPB_to)
result_plasma <- formula_convertConc(result_unbound, from = "unbound", to = "plasma", 
                                     Rbp_from, PPB_from, Rbp_to, PPB_to)

print(c(result_plasma, expected_plasma))
  
expected_plasma <- Cin * (1 - PPB_from) / (1 - PPB_to) / Rbp_from
result_plasma <- formula_convertConc(Cin, from = "blood", to = "plasma", 
                                     Rbp_from, PPB_from, Rbp_to, PPB_to)

print(c(result_plasma, expected_plasma))
}
\seealso{
Other Translational: 
\code{\link{formula_CLhPlToCLintu}()},
\code{\link{formula_CLhToCLintu}()},
\code{\link{formula_CLintuToCLh}()},
\code{\link{formula_CLintuToCLhPl}()},
\code{\link{formula_CLintuToCLh_Parallel}()},
\code{\link{formula_CLintuToCLh_WellStirred}()},
\code{\link{formula_HepExtraction}()}
}
\author{
Anne Kuemmel (IntiQuan), Venelin Mitov (IntiQuan)
}
\concept{Translational}
